# ThreadMesh - pip requirements
# All packages AGPL-3.0-or-later compatible
# Install: pip install threadmesh
# Launch:  threadmesh

# --- Core GUI & Visualization ---
PySide6>=6.6.0
vtk>=9.3.0

# --- Mesh I/O ---
meshio>=5.3.0
h5py>=3.10.0

# --- Mesh Generation ---
gmsh>=4.12.0

# --- Mesh Repair & Processing ---
pymeshlab>=2023.12

# --- Core Numerical ---
numpy>=1.26.0
scipy>=1.12.0

# --- JIT Compilation & CPU/GPU Acceleration ---
numba>=0.59.0

# --- Visualization / Plotting ---
matplotlib>=3.8.0

# --- System Monitoring (CPU cores, RAM) ---
psutil>=5.9.0

# --- GPU Detection (optional - gracefully skipped if unavailable) ---
GPUtil>=1.4.0
nvidia-ml-py

# --- GPU Compute (optional - install manually based on platform) ---
# NVIDIA CUDA (Linux/Windows only):
#   pip install cupy-cuda12x   (for CUDA 12.x)
#   pip install cupy-cuda11x   (for CUDA 11.x)
# OpenCL cross-vendor (Linux/Windows only):
#   pip install pyopencl
# macOS: GPU acceleration not available (CUDA/OpenCL deprecated)
# All GPU packages fail gracefully - ThreadMesh falls back to CPU automatically
